"use client";
import React, { useEffect, useState } from "react";
import SettingsModal from "../modals/settings-modal";

const ModalProvider = () => {
    const [mounted, setMounted] = useState(false);

    useEffect(() => {
        setMounted(true);
    }, []);

    if (!mounted) return null;//mountedda boshida false bor yani useEffect user saytga kirgandan ishlaydi agar user kirmasa ????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????

    return (
        <>
            <SettingsModal />
        </>
    );
};

export default ModalProvider;
